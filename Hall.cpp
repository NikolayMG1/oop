#include "Hall.h"//including Hall.h so we can access the class Hall

Hall::Hall(const unsigned number, const unsigned rows, const unsigned spaces){
    this->number = number;
    this->rows = rows;
    this->spaces = spaces;
}